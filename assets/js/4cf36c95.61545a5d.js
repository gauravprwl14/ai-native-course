"use strict";(globalThis.webpackChunkwebsite=globalThis.webpackChunkwebsite||[]).push([[194],{97732(e,n,t){t.r(n),t.d(n,{assets:()=>a,contentTitle:()=>c,default:()=>u,frontMatter:()=>o,metadata:()=>r,toc:()=>l});const r=JSON.parse('{"id":"tier-2-builder/rag-core/patterns","title":"Patterns: RAG Pipelines","description":"Practical RAG pipeline patterns \u2014 from basic in-memory retrieval to source citation, fallback handling, and multi-document indexing.","source":"@site/docs/tier-2-builder/14-rag-core/patterns.mdx","sourceDirName":"tier-2-builder/14-rag-core","slug":"/tier-2-builder/rag-core/patterns","permalink":"/ai-native-course/tier-2-builder/rag-core/patterns","draft":false,"unlisted":false,"tags":[],"version":"current","lastUpdatedAt":1774637831000,"sidebarPosition":3,"frontMatter":{"title":"Patterns: RAG Pipelines","sidebar_position":3,"description":"Practical RAG pipeline patterns \u2014 from basic in-memory retrieval to source citation, fallback handling, and multi-document indexing."},"sidebar":"courseSidebar","previous":{"title":"Concepts: Retrieval-Augmented Generation","permalink":"/ai-native-course/tier-2-builder/rag-core/concepts"},"next":{"title":"Lab: Build a Document Q&A with RAG","permalink":"/ai-native-course/tier-2-builder/rag-core/lab"}}');var s=t(74848),i=t(28453);const o={title:"Patterns: RAG Pipelines",sidebar_position:3,description:"Practical RAG pipeline patterns \u2014 from basic in-memory retrieval to source citation, fallback handling, and multi-document indexing."},c="Patterns: RAG Pipelines",a={},l=[{value:"Pattern 1: Basic RAG Pipeline",id:"pattern-1-basic-rag-pipeline",level:2},{value:"Pattern 2: RAG with Source Citation",id:"pattern-2-rag-with-source-citation",level:2},{value:"Pattern 3: Fallback When No Relevant Docs",id:"pattern-3-fallback-when-no-relevant-docs",level:2},{value:"Pattern 4: Multi-Document RAG",id:"pattern-4-multi-document-rag",level:2},{value:"Anti-Patterns",id:"anti-patterns",level:2}];function d(e){const n={code:"code",h1:"h1",h2:"h2",header:"header",hr:"hr",mermaid:"mermaid",p:"p",pre:"pre",strong:"strong",...(0,i.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"patterns-rag-pipelines",children:"Patterns: RAG Pipelines"})}),"\n",(0,s.jsx)(n.h2,{id:"pattern-1-basic-rag-pipeline",children:"Pattern 1: Basic RAG Pipeline"}),"\n",(0,s.jsx)(n.p,{children:"The simplest working RAG: an in-memory index built with cosine similarity. No external dependencies. Good for learning, prototyping, and small corpora (< 10,000 chunks)."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import math\nimport sys\nfrom pathlib import Path\nsys.path.insert(0, str(Path(__file__).parent.parent.parent.parent.parent / "shared"))\n\nfrom utils import get_anthropic_client, get_openai_client\n\nEMBEDDING_MODEL = "text-embedding-3-small"\nLLM_MODEL = "claude-3-haiku-20240307"\n\nSYSTEM_PROMPT = """You are a helpful assistant that answers questions based only on the provided context.\nIf the context does not contain the answer, say so clearly."""\n\n\ndef cosine_similarity(v1: list[float], v2: list[float]) -> float:\n    dot = sum(a * b for a, b in zip(v1, v2))\n    mag1 = math.sqrt(sum(a ** 2 for a in v1))\n    mag2 = math.sqrt(sum(b ** 2 for b in v2))\n    if mag1 == 0 or mag2 == 0:\n        return 0.0\n    return dot / (mag1 * mag2)\n\n\ndef build_index(documents: list[dict]) -> list[dict]:\n    """\n    Build in-memory vector index from documents.\n    Each document: {"id": str, "text": str, "source": str}\n    """\n    openai_client = get_openai_client()\n    indexed = []\n    for doc in documents:\n        response = openai_client.embeddings.create(\n            model=EMBEDDING_MODEL,\n            input=doc["text"]\n        )\n        indexed.append({\n            **doc,\n            "embedding": response.data[0].embedding\n        })\n    return indexed\n\n\ndef retrieve_chunks(query: str, index: list[dict], top_k: int = 3) -> list[dict]:\n    """Find the top_k chunks most similar to the query."""\n    openai_client = get_openai_client()\n    response = openai_client.embeddings.create(\n        model=EMBEDDING_MODEL,\n        input=query\n    )\n    query_vector = response.data[0].embedding\n\n    scored = []\n    for chunk in index:\n        sim = cosine_similarity(query_vector, chunk["embedding"])\n        scored.append({**chunk, "similarity": sim})\n\n    scored.sort(key=lambda x: x["similarity"], reverse=True)\n    return scored[:top_k]\n\n\ndef answer_question(question: str, index: list[dict], top_k: int = 3) -> dict:\n    """Answer a question using RAG."""\n    chunks = retrieve_chunks(question, index, top_k)\n\n    context = "\\n\\n".join(\n        f"[Source: {c[\'source\']}]\\n{c[\'text\']}" for c in chunks\n    )\n\n    prompt = f"""Context documents:\n{context}\n\nQuestion: {question}\n\nAnswer based only on the context above:"""\n\n    client = get_anthropic_client()\n    response = client.messages.create(\n        model=LLM_MODEL,\n        max_tokens=512,\n        system=SYSTEM_PROMPT,\n        messages=[{"role": "user", "content": prompt}]\n    )\n\n    sources = list({c["source"] for c in chunks})\n    return {\n        "answer": response.content[0].text,\n        "sources": sources,\n        "chunks_used": top_k\n    }\n\n\n# --- Usage ---\ndocs = [\n    {"id": "1", "text": "Enterprise customers get a 60-day refund window.", "source": "refund-policy.txt"},\n    {"id": "2", "text": "Standard accounts have a 30-day refund window.", "source": "refund-policy.txt"},\n    {"id": "3", "text": "Refunds are processed within 5\u20137 business days.", "source": "refund-policy.txt"},\n]\n\nindex = build_index(docs)\nresult = answer_question("How long do enterprise customers have to request a refund?", index)\nprint(result["answer"])\nprint("Sources:", result["sources"])\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"When to use:"})," Prototyping, small document sets, learning RAG fundamentals."]}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"Limitation:"})," No similarity threshold \u2014 will always return ",(0,s.jsx)(n.code,{children:"top_k"})," chunks even if they're irrelevant."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"pattern-2-rag-with-source-citation",children:"Pattern 2: RAG with Source Citation"}),"\n",(0,s.jsx)(n.p,{children:"Include document metadata in context and ask the model to explicitly cite sources. Critical for production use where users need to verify answers."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'CITATION_SYSTEM_PROMPT = """You are a helpful assistant that answers questions based only on the provided context.\nAlways cite which document your answer comes from using the [Source: ...] label.\nIf the context does not contain the answer, say "I don\'t have information about that in the provided documents."\n"""\n\nCITATION_PROMPT_TEMPLATE = """Context documents:\n{context}\n\nQuestion: {question}\n\nInstructions: Answer the question using only the context above. For each claim you make, cite the source document in parentheses like: (Source: document-name.txt)."""\n\n\ndef answer_with_citations(question: str, index: list[dict], top_k: int = 3) -> dict:\n    """Answer a question and return structured citations."""\n    chunks = retrieve_chunks(question, index, top_k)\n\n    context_parts = []\n    for i, chunk in enumerate(chunks, 1):\n        context_parts.append(\n            f"[Document {i} \u2014 Source: {chunk[\'source\']}]\\n{chunk[\'text\']}"\n        )\n    context = "\\n\\n".join(context_parts)\n\n    prompt = CITATION_PROMPT_TEMPLATE.format(context=context, question=question)\n\n    client = get_anthropic_client()\n    response = client.messages.create(\n        model=LLM_MODEL,\n        max_tokens=512,\n        system=CITATION_SYSTEM_PROMPT,\n        messages=[{"role": "user", "content": prompt}]\n    )\n\n    return {\n        "answer": response.content[0].text,\n        "sources": [c["source"] for c in chunks],\n        "similarity_scores": [round(c["similarity"], 3) for c in chunks],\n        "chunks_used": len(chunks)\n    }\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"When to use:"})," Any production RAG where users need to verify answers or trust is important (legal, medical, compliance, customer support)."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"pattern-3-fallback-when-no-relevant-docs",children:"Pattern 3: Fallback When No Relevant Docs"}),"\n",(0,s.jsx)(n.p,{children:"Check similarity scores before calling the LLM. If no chunk is similar enough, respond with a graceful fallback instead of hallucinating from irrelevant context."}),"\n",(0,s.jsx)(n.mermaid,{value:"graph TD\n  Q[User Question] --\x3e E[Embed Query]\n  E --\x3e S[Search Index]\n  S --\x3e T{Best similarity > threshold?}\n  T --\x3e|Yes| LLM[Call LLM with context]\n  T --\x3e|No| F[Return fallback message]\n  LLM --\x3e A[Answer with sources]\n  F --\x3e A2[No relevant documents found]"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'SIMILARITY_THRESHOLD = 0.70\n\n\ndef answer_with_fallback(\n    question: str,\n    index: list[dict],\n    top_k: int = 3,\n    threshold: float = SIMILARITY_THRESHOLD\n) -> dict:\n    """Answer with graceful fallback when no relevant docs are found."""\n    chunks = retrieve_chunks(question, index, top_k)\n\n    # Filter to chunks above the similarity threshold\n    relevant_chunks = [c for c in chunks if c["similarity"] >= threshold]\n\n    if not relevant_chunks:\n        return {\n            "answer": "I don\'t have information about that topic in the provided documents.",\n            "sources": [],\n            "chunks_used": 0,\n            "fallback": True\n        }\n\n    context = "\\n\\n".join(\n        f"[Source: {c[\'source\']}]\\n{c[\'text\']}" for c in relevant_chunks\n    )\n\n    prompt = f"""Context documents:\n{context}\n\nQuestion: {question}\n\nAnswer based only on the context above:"""\n\n    client = get_anthropic_client()\n    response = client.messages.create(\n        model=LLM_MODEL,\n        max_tokens=512,\n        system=SYSTEM_PROMPT,\n        messages=[{"role": "user", "content": prompt}]\n    )\n\n    return {\n        "answer": response.content[0].text,\n        "sources": list({c["source"] for c in relevant_chunks}),\n        "chunks_used": len(relevant_chunks),\n        "fallback": False\n    }\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"When to use:"})," Always in production. Without a threshold, RAG confidently answers from irrelevant context \u2014 the worst type of hallucination because it looks grounded."]}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"Tuning the threshold:"})," Start at 0.70. Increase if you're getting irrelevant answers; decrease if you're getting too many fallbacks."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"pattern-4-multi-document-rag",children:"Pattern 4: Multi-Document RAG"}),"\n",(0,s.jsx)(n.p,{children:"Index multiple documents and preserve metadata tags for filtering. Useful when you want to narrow retrieval to a specific document type or category."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'from datetime import datetime\n\n\ndef build_multi_doc_index(document_sources: list[dict]) -> list[dict]:\n    """\n    Index multiple documents with rich metadata.\n    Each source: {"path": str, "text": str, "source": str, "category": str, "date": str}\n    """\n    openai_client = get_openai_client()\n    index = []\n\n    for doc in document_sources:\n        # Simple sentence-boundary chunking\n        sentences = doc["text"].split(". ")\n        chunks = []\n        current = ""\n        for sentence in sentences:\n            if len(current.split()) + len(sentence.split()) < 150:\n                current = (current + ". " + sentence).strip(". ")\n            else:\n                if current:\n                    chunks.append(current)\n                current = sentence\n        if current:\n            chunks.append(current)\n\n        for i, chunk_text in enumerate(chunks):\n            response = openai_client.embeddings.create(\n                model=EMBEDDING_MODEL,\n                input=chunk_text\n            )\n            index.append({\n                "id": f"{doc[\'source\']}::{i}",\n                "text": chunk_text,\n                "source": doc["source"],\n                "category": doc.get("category", "general"),\n                "date": doc.get("date", ""),\n                "embedding": response.data[0].embedding\n            })\n\n    return index\n\n\ndef retrieve_by_category(\n    query: str,\n    index: list[dict],\n    category: str | None = None,\n    top_k: int = 3\n) -> list[dict]:\n    """Retrieve chunks, optionally filtered to a specific document category."""\n    filtered_index = index\n    if category:\n        filtered_index = [c for c in index if c.get("category") == category]\n\n    return retrieve_chunks(query, filtered_index, top_k)\n\n\n# --- Usage ---\nsources = [\n    {\n        "text": "Enterprise refund window is 60 days. Standard is 30 days.",\n        "source": "refund-policy.txt",\n        "category": "policies",\n        "date": "2024-01-01"\n    },\n    {\n        "text": "Python SDK v2.0 supports async calls and streaming.",\n        "source": "sdk-changelog.txt",\n        "category": "technical",\n        "date": "2024-03-15"\n    },\n]\n\nmulti_index = build_multi_doc_index(sources)\n\n# Query only policy documents\npolicy_chunks = retrieve_by_category(\n    "What is the refund policy?",\n    multi_index,\n    category="policies"\n)\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"When to use:"})," Document collections with distinct categories (policies vs. technical docs vs. FAQs). Category filtering prevents cross-contamination between domains."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"anti-patterns",children:"Anti-Patterns"}),"\n",(0,s.jsxs)("div",{className:"antipattern",children:[(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Chunk size too large (1000+ tokens)"})}),(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:"# Bad \u2014 retrieves too much context, dilutes relevance\nchunks = split_by_tokens(text, chunk_size=1000)\n\n# Good \u2014 smaller chunks, more precise retrieval\nchunks = split_by_tokens(text, chunk_size=300, overlap=50)\n"})}),(0,s.jsx)(n.p,{children:'Large chunks average out similarity scores across many topics. A chunk about "refunds, shipping, and returns" will match weakly on any single topic. Smaller, focused chunks retrieve more precisely.'}),(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"No similarity threshold"})}),(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'# Bad \u2014 always returns top_k even if they\'re irrelevant\nresults = retrieve_chunks(query, index, top_k=3)\nanswer = call_llm(context=results)  # answers from garbage context\n\n# Good \u2014 filter below threshold, return fallback\nresults = retrieve_chunks(query, index, top_k=3)\nrelevant = [r for r in results if r["similarity"] > 0.70]\nif not relevant:\n    return "I don\'t have information about that topic."\n'})}),(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Injecting raw chunks without source labels"})}),(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'# Bad \u2014 LLM cannot cite sources it wasn\'t told about\ncontext = "\\n\\n".join(chunk["text"] for chunk in chunks)\n\n# Good \u2014 label each chunk with its source\ncontext = "\\n\\n".join(\n    f"[Source: {c[\'source\']}]\\n{c[\'text\']}" for c in chunks\n)\n'})}),(0,s.jsx)(n.p,{children:"Without source labels, the model synthesises an answer but cannot tell the user where it came from. This breaks trust and makes verification impossible."})]})]})}function u(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(d,{...e})}):d(e)}},28453(e,n,t){t.d(n,{R:()=>o,x:()=>c});var r=t(96540);const s={},i=r.createContext(s);function o(e){const n=r.useContext(i);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:o(e.components),r.createElement(i.Provider,{value:n},e.children)}}}]);